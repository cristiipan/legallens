# Redis replica config. Points at the master service by docker-compose name.
# Promotion to master is handled by Sentinel.

bind 0.0.0.0
port 6379
protected-mode no

replicaof redis-master 6379
replica-read-only yes

appendonly yes
appendfsync everysec
